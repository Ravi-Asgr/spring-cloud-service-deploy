server:
  address: 0.0.0.0
  #port config
  port: ${PORT:8080}

  tomcat:
    accesslog:
      enabled: true
      pattern: "%h %l %u %t \"%r\" %s %b"
      
management:
  endpoints:
    web:
      exposure:
        include: health,info     
