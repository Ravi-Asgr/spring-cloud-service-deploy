server:
  address: 0.0.0.0
  port: ${PORT:8080}

server:
  tomcat:
    accesslog:
      enabled: true
      pattern: "%h %l %u %t \"%r\" %s %b"
      
management:
  endpoints:
    web:
      exposure:
        include: health,info     